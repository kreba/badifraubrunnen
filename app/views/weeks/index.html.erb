<% content_for :title, @title = t('weeks.index.title') %>
<% content_for :dynamic_links_for_webmaster do %>
  <%= navigation_link t('weeks.new.title'), new_week_path %>
<% end %>


<%= render :partial => 'weeks_table' %>

<%= render( :partial => 'weeks_past_view' ) unless @past_weeks.empty?  %>
