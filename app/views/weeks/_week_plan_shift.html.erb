<span class="week_plan_shift" style="<%= @dd.style_for_shift(shift) %>">
	<% if shift.free? %>
		<% if current_person.is_staff_for?(@saison) or current_person.is_admin_for?(@saison) %>
			<%= block_link_to( t('shifts.sign_up') , edit_shift_path(shift), :padding => "15px 3px" ) %>
		<% else %>
			vakant
		<% end unless printout %>
	<% else %>
		<%= shift.person.name %>
        <%# <br /><%= phone_str(shift.person) if printout %>
	<% end %>
</span> 