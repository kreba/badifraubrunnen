<% if !!person.postal_code and !person.address.blank? %>
	<%= link_to( person.full_address_str(delimiter: '<br />'), "http://map.search.ch/#{person.postal_code}/#{person.address.untaint}",
							 title: t('people.show_map'), popup: true )%>
<% else %>
	<%= person.full_address_str %>
<% end %>
