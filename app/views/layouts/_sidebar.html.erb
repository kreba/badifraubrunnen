<div id="side">

  <h3><%=l 'navigation' %></h3>

	<% if logged_in? %> 
    <%= render( :partial => 'layouts/member_navigation' )%>	
	<% else %>
		<ul>
			<li><%= link_to_unless_current( l('sessions','login'), login_path )%></li>
		  <li><%= link_to_unless_current( l('help','title'), help_path )%></li>
		</ul>
	<% end %>

</div>
